import { useState } from  'react';
interface OptionsType
{
  slides: number;
}

function EmblaCarousel({ options }: Props) {
  const [callbackFn] = useState(() => {});

  return <div>EmblaCarousel</div>;
}

export default EmblaCarousel;
