# Database Configuration
# Example: postgresql://username:password@localhost:5432/database_name
# For local development, you can use PostgreSQL or a cloud provider like Supabase
DATABASE_URL="postgresql://postgres:password@localhost:5432/modern_portfolio_blog"

# Next.js Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-here"
NEXT_PUBLIC_SITE_URL="https://yoursite.com"

# Email Configuration (for contact forms)
RESEND_API_KEY="your_resend_api_key_here"
CONTACT_EMAIL="your_contact_email@example.com"
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
FROM_EMAIL="your-email@gmail.com"

# Analytics (Optional)
VERCEL_ANALYTICS_ID="your_vercel_analytics_id"

# External APIs (Optional)
OPENAI_API_KEY="sk-your-openai-api-key" # For AI-powered content suggestions

# Security Configuration (CRITICAL - Use strong values in production)
JWT_SECRET="your-super-secure-jwt-secret-key-minimum-32-characters-long-for-development-only"
JWT_EXPIRES_IN="1h" # Token expiration time (1h, 30m, 7d, etc.)
ADMIN_API_TOKEN="your-secure-admin-api-token-minimum-32-characters-long" # Fallback admin token

# Production Security Requirements:
# - JWT_SECRET: Minimum 64 characters, cryptographically random
# - ADMIN_API_TOKEN: Minimum 64 characters, unique per environment
# - NEXT_PUBLIC_SITE_URL: Must use HTTPS in production
# - Never use default, test, dev, or common words in secrets

# Environment
NODE_ENV="development"