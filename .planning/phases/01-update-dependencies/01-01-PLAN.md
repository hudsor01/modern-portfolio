---
phase: 01-update-dependencies
plan: 01
type: execute
---

<objective>
Update 6 outdated packages to latest patch/minor versions for security and stability.

Purpose: Ensure dependencies receive latest bug fixes and security patches while maintaining compatibility with existing codebase.
Output: Updated package.json and lockfile with all 913 tests passing, confirming zero-downtime upgrade.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@package.json

**Constraint from PROJECT.md:**
- Bun runtime required for all commands (not npm/yarn)
- All 913 tests must pass after updates
- Zero downtime - changes must be deployable without service interruption

**Dependencies to update (from CONCERNS.md):**
- `motion`: 12.24.7 → 12.24.12 (5 patch versions behind)
- `react-error-boundary`: 6.0.2 → 6.0.3 (1 patch)
- `react-resizable-panels`: 4.3.0 → 4.3.2 (2 patches)
- `resend`: 6.6.0 → 6.7.0 (1 minor)
- `@happy-dom/global-registrator`: 20.0.11 → 20.1.0 (dev dep)
- `happy-dom`: 20.0.11 → 20.1.0 (dev dep)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 6 outdated packages to latest versions</name>
  <files>package.json, bun.lockb</files>
  <action>Update packages using Bun (not npm). Run: `bun add motion@latest react-error-boundary@latest react-resizable-panels@latest resend@latest` for production dependencies, then `bun add -d happy-dom@latest @happy-dom/global-registrator@latest` for dev dependencies. Use @latest flag to get current stable versions. Bun will automatically update bun.lockb. Verify package.json shows updated versions after completion.</action>
  <verify>Check package.json shows updated versions for all 6 packages. Run `bun install` to ensure lockfile is consistent.</verify>
  <done>All 6 packages updated in package.json with new versions, bun.lockb regenerated with no conflicts.</done>
</task>

<task type="auto">
  <name>Task 2: Verify test stability after dependency updates</name>
  <files>None (verification only)</files>
  <action>Run full test suite with `bun test` to confirm all 913 tests still pass. If any tests fail, investigate whether it's due to updated dependencies. Check for breaking changes in changelogs if failures occur. All packages are patch/minor updates so breaking changes are unlikely, but verify behavior hasn't changed.</action>
  <verify>Run `bun test` - must show 913 passing tests, 62 skipped (same as before updates). Run `bun run type-check` - must complete with 0 errors. Run `bun run lint` - must complete with same warnings as before (7 warnings expected).</verify>
  <done>All 913 tests pass, type checking succeeds, no new errors or warnings introduced by dependency updates.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun test` shows 913 passing tests, 62 skipped
- [ ] `bun run type-check` completes with 0 errors
- [ ] `bun run lint` shows no new warnings
- [ ] `bun run build` succeeds without errors
- [ ] All 6 packages show updated versions in package.json
</verification>

<success_criteria>

- All tasks completed
- All 6 packages updated to latest patch/minor versions
- All verification checks pass
- No breaking changes introduced
- Zero test failures
- Type safety maintained
</success_criteria>

<output>
After completion, create `.planning/phases/01-update-dependencies/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Update Dependencies Summary

**Updated 6 outdated packages to latest stable versions with zero test failures**

## Accomplishments

- Updated motion from 12.24.7 → 12.24.12 (5 patch releases)
- Updated react-error-boundary from 6.0.2 → 6.0.3 (1 patch)
- Updated react-resizable-panels from 4.3.0 → 4.3.2 (2 patches)
- Updated resend from 6.6.0 → 6.7.0 (1 minor)
- Updated happy-dom from 20.0.11 → 20.1.0 (1 minor, dev dep)
- Updated @happy-dom/global-registrator from 20.0.11 → 20.1.0 (1 minor, dev dep)
- All 913 tests passing after updates
- Zero breaking changes detected

## Files Created/Modified

- `package.json` - Updated 6 dependency versions
- `bun.lockb` - Regenerated with updated dependency tree

## Decisions Made

None - straightforward patch/minor updates with no architectural decisions.

## Issues Encountered

[Document any test failures, compatibility issues, or unexpected behavior, or "None"]

## Next Phase Readiness

Phase 1 complete. Ready for Phase 2: Implement Nonce-Based CSP.
No blockers or concerns for next phase.
</output>
